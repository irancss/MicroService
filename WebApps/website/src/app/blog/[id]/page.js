export default function Slug({params}) {
    return (
        <div>
            <h1>Slug</h1>
            <p>Slug: {params.id}</p>
            <p>Slug page content goes here.</p>
        </div>
    );
}